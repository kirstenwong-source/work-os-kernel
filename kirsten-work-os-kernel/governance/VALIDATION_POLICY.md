# Validation Policy

Validation exists to keep the kernel coherent.

The validator checks:

- required paths
- JSON parsing
- registry targets
- schema required fields
- branch contract references
- example object shape

Validation does not prove that every career claim is true. Evidence quality still requires human review.
